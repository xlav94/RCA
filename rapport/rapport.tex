\documentclass[final]{article}
\usepackage{svg}
\usepackage{float}

% if you need to pass options to natbib, use, e.g.:
%     \PassOptionsToPackage{numbers, compress}{natbib}
% before loading neurips_2025

% The authors should use one of these tracks.
% Before accepting by the NeurIPS conference, select one of the options below.
% 0. "default" for submission
% the "default" option is equal to the "main" option, which is used for the Main Track with double-blind reviewing.
% 1. "main" option is used for the Main Track
%  \usepackage[main]{neurips_2025}
% 2. "position" option is used for the Position Paper Track
%  \usepackage[position]{neurips_2025}
% 3. "dandb" option is used for the Datasets & Benchmarks Track
 % \usepackage[dandb]{neurips_2025}
% 4. "creativeai" option is used for the Creative AI Track
%  \usepackage[creativeai]{neurips_2025}
% 5. "sglblindworkshop" option is used for the Workshop with single-blind reviewing
 % \usepackage[sglblindworkshop]{neurips_2025}
% 6. "dblblindworkshop" option is used for the Workshop with double-blind reviewing
%  \usepackage[dblblindworkshop]{neurips_2025}

% After being accepted, the authors should add "final" behind the track to compile a camera-ready version.
% 1. Main Track
 % \usepackage[main, final]{neurips_2025}
% 2. Position Paper Track
%  \usepackage[position, final]{neurips_2025}
% 3. Datasets & Benchmarks Track
 % \usepackage[dandb, final]{neurips_2025}
% 4. Creative AI Track
%  \usepackage[creativeai, final]{neurips_2025}
% 5. Workshop with single-blind reviewing
%  \usepackage[sglblindworkshop, final]{neurips_2025}
% 6. Workshop with double-blind reviewing
%  \usepackage[dblblindworkshop, final]{neurips_2025}
% Note. For the workshop paper template, both \title{} and \workshoptitle{} are required, with the former indicating the paper title shown in the title and the latter indicating the workshop title displayed in the footnote.
% For workshops (5., 6.), the authors should add the name of the workshop, "\workshoptitle" command is used to set the workshop title.
% \workshoptitle{WORKSHOP TITLE}

% "preprint" option is used for arXiv or other preprint submissions
 \usepackage[preprint]{neurips_2025}

% to avoid loading the natbib package, add option nonatbib:
%    \usepackage[nonatbib]{neurips_2025}

\usepackage[utf8]{inputenc} % allow utf-8 input
\usepackage[T1]{fontenc}    % use 8-bit T1 fonts
\usepackage{hyperref}       % hyperlinks
\usepackage{url}            % simple URL typesetting
\usepackage{booktabs}       % professional-quality tables
\usepackage{amsfonts}       % blackboard math symbols
\usepackage{nicefrac}       % compact symbols for 1/2, etc.
\usepackage{microtype}      % microtypography
\usepackage{xcolor}
\usepackage{graphicx}         % colors
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, calc, backgrounds, fit}
\usepackage{amsmath}
% Note. For the workshop paper template, both \title{} and \workshoptitle{} are required, with the former indicating the paper title shown in the title and the latter indicating the workshop title displayed in the footnote.
\title{Deep Reinforcement Learning for Portfolio Allocation: A Post-Modern Portfolio Theory Approach}


% The \author macro works with any number of authors. There are two commands
% used to separate the names and addresses of multiple authors: \And and \AND.
%
% Using \And between authors leaves it to LaTeX to determine where to break the
% lines. Using \AND forces a line break at that point. So, if LaTeX puts 3 of 4
% authors names on the first line, and the last on the second line, try using
% \AND instead of \And before the third author name.


\author{
    Cédric Guévremont \\
    Université de Montréal \\
    Matricule : 20266532 \\
    \texttt{cedric.guevremont@umontreal.ca}
    \And
    Rima Boujenane \\
    Université de Montréal \\
     Matricule : 20235550 \\
    \texttt{rima.boujenane@umontreal.ca}
    \And
    Abdelghafour Rahmouni \\
    Université de Montréal \\
    Matricule : 20246224 \\
    \texttt{abdel.ghafour.rahmouni@umontreal.ca}
}

\begin{document}


\maketitle
\section{Introduction}
Portfolio management has traditionally been guided by Modern Portfolio Theory (MPT), which aims to maximize returns for
a given level of risk through diversification. Post-Modern Portfolio Theory (PMPT) extends this idea by distinguishing
between desirable and undesirable fluctuations, focusing specifically on downside risk rather than overall volatility.
Both approaches, however, rely on static optimization and do not adapt well to the constantly changing nature of
financial markets.\\\\
At the same time, recent advances in machine learning, particularly in reinforcement learning, have introduced more
adaptive approaches to portfolio management. However, many of these methods lack a strong grounding in financial theory,
often prioritizing performance without explicitly controlling for risk, leaving a gap between adaptability and disciplined
risk management.\\\\
In this project, we study a reinforcement learning agent designed to manage a portfolio composed of a limited number of
financial assets and an initial capital. At each time step, the agent observes the market through a sliding window of
historical price data, transformed using fractional differentiation to retain long-term dependencies while ensuring
stationarity. Based on this information, the agent adjusts the portfolio allocation across assets, and its decisions are
evaluated through a reward function that accounts for both returns and risk.\\\\
To bridge the gap between static financial theory and adaptive learning, we introduce a hybrid approach, in which the
agent's decisions are passed through an optimization layer that implements either a Mean-Variance (MPT) or a
Mean-Semi-deviation (PMPT) objective, so that the final allocations explicitly balance expected returns with risk
considerations. This integration allows us to leverage the adaptability of reinforcement learning while ensuring that
the agent's behavior remains consistent with established financial principles. The learning process is based on the
Proximal Policy Optimization (PPO) algorithm, an on-policy actor-critic method in which the actor suggests actions while
the critic evaluates their quality and guides the learning process. PPO's ability to constrain policy updates ensures
stable and progressive improvements without unlearning previously acquired knowledge.\\\\
The main objective of this project is to evaluate the extent to which the combination of recurrent neural networks
(RNN/LSTM) and reinforcement learning can reduce the overall risk of a financial portfolio in a sequential decision-making
context, while maintaining competitive returns compared to traditional benchmarks.\\\\
The remainder of this paper is organized as follows. Section 2 reviews related work. Section 3 presents the proposed
architecture. Section 4 describes the data, feature engineering, and training setup. Section 5 discusses the results.
Finally, Section 6 concludes and outlines directions for future work.

\section{Related Works}

\subsection{Foundational Financial Theories}

Traditional portfolio optimization methods, such as Markowitz's Modern Portfolio Theory (MPT) \citep{markowitz1952portfolio}, have been the industry standard for decades to balance risk and return through diversification. While effective in theory, MPT relies on static rules that often struggle to keep up with the chaotic, fast-moving reality of today's markets.

With the rise of machine learning, new approaches have emerged to tackle these limitations. \citet{jang2023deep} proposed integrating Deep Reinforcement Learning (DRL) with Technical Analysis and Modern Portfolio Theory constraints. Unlike traditional supervised learning models that merely predict prices, DRL agents learn sequential decision-making policies directly through interaction with the market environment. This transition from static allocation to dynamic portfolio management is emphasized in the comprehensive survey by \citet{pippas2025evolution}, which traces the evolution of reinforcement learning techniques in quantitative finance.

\subsection{Sequential Modeling and Temporal Dependencies}

Addressing the partial observability of financial markets, \citet{li2015recurrent} introduced a hybrid recurrent reinforcement learning framework integrating RNN-LSTM architectures within a Q-learning setup. Their approach enables agents to maintain memory of historical market states, capturing temporal dependencies that are critical in stochastic and non-stationary environments.

\subsection{The Evolution of Deep Reinforcement Learning in Finance}

Building upon classical frameworks, \citet{jang2023deep} combined DRL with MPT constraints using tensor decomposition techniques to manage high-dimensional financial data within structured risk frameworks. Similarly, \citet{jiang2017deep} proposed a CNN-based reinforcement learning framework that automates feature extraction from multi-channel price representations, reducing reliance on handcrafted indicators and improving scalability.

\subsection{Actor-Critic Frameworks}

Modern portfolio optimization strategies predominantly rely on the Actor-Critic architecture, which simultaneously updates a policy network (Actor) and a value function estimator (Critic). As implemented in the FinRL framework \citep{liu2020finrl}, in the ensemble strategy proposed by \citet{yang2025ensemble}, and discussed in \citet{jang2023deep}, actor-critic methods provide improved stability and continuous control capabilities.

\begin{itemize}
    \item \textbf{DDPG} (Deep Deterministic Policy Gradient) enables continuous portfolio weight allocation and deterministic policy updates.
    \item \textbf{A2C} (Advantage Actor-Critic) reduces gradient variance using an advantage function, improving robustness in volatile markets.
    \item \textbf{PPO} (Proximal Policy Optimization) stabilizes training by constraining policy updates through clipped objective functions.
\end{itemize}

\subsection{Alternative Data and Future Directions}

Recent work incorporates alternative data streams to enrich state representations. \citet{nawathe2024multimodal} integrated financial news sentiment using NLP models such as FinBERT into DRL-based portfolio optimization. Emerging research by \citet{liu2025quantum} explores quantum-enhanced reinforcement learning combined with LSTM forecasting signals to improve convergence stability and performance in noisy financial systems.

\subsection{Risk-Aware Rewards and Responsible Management}

To ensure robust and responsible financial decision-making, recent literature emphasizes risk-aware reward functions. Both the FinRL framework \citep{liu2020finrl} and the ensemble strategy proposed by \citet{yang2025ensemble} incorporate market frictions such as transaction costs and financial turbulence indices. These mechanisms penalize excessive speculation and enable agents to survive extreme market conditions, including financial crashes.

\newpage

\section{Methods}
This section describes the main components of the proposed system: the environment and data pipeline, the feature
engineering process, and the learning architecture, including the integration of the optimization layer and the reward
function design.
Together, these elements form an end-to-end pipeline in which a reinforcement learning agent learns to allocate capital
across a diversified portfolio of financial assets, in a way that balances adaptibility and established financial principles.

\subsection{Environment and Data Pipeline}
The portfolio management problem is formulated as a sequential decision-making task within a custom environment
compatible with the \texttt{Gymnasium} interface. The environment simulates the evolution of a portfolio composed of 21
highly capitalized financial assets, spanning 7 sectors, selected to ensure broad sectoral diversification, as detailed
in Table~\ref{tab:portfolio_assets}.
Historical daily opening prices are retrieved via the Yahoo Finance API, covering the period from January 2010 to
February 2026. To ensure chronological integrity and prevent data leakage, the data undergoes a strict time-series
split: the first 80\% of trading days form the training set, while the remaining 20\% are reserved for out-of-sample test set.

\begin{table}[h!]
    \centering
    \caption{Portfolio assets by sector.}
    \label{tab:portfolio_assets}
    \begin{tabular}{ll}
        \toprule
        \textbf{Assets} & \textbf{Sector} \\
        \midrule
        MSFT, GOOG, AMZN, AAPL & Technology \\
        BRK-B, JPM, V & Financials \\
        WMT, COST, PG & Consumer Staples \\
        LLY, UNH & Healthcare \\
        GLD, DBC & Commodities \\
        UNP, CAT, LIN, NEE, AMT & Industrials \\
        XOM, CVX & Energy \\
        \bottomrule
    \end{tabular}
\end{table}

At each time step $t$, the agent receives an observation composed of two components: a market history matrix of dimension
$W \times N$, containing the price series of all $N = 21$ assets over a rolling window of $W = 60$ trading days, enriched with
fractionally differentiated features described in Section~\ref{sec:fracdiff}; and a portfolio state vector
$\mathbf{w}_t \in [0, 1]^N$ describing the current allocation weights across assets. The action space is continuous, with
the actor outputting a vector $\mathbf{a}_t \in [-1, 1]^N$ interpreted as expected returns, which is then passed through
a financial optimization layer to produce the final portfolio weights. The reward signal is described in Section~\ref{sec:reward}.

\subsection{Feature Engineering: Fractional Differentiation}
\label{sec:fracdiff}
Raw asset prices present a fundamental challenge for learning models: they are non-stationary, meaning their statistical
properties drift over time, making it difficult for the agent to generalize across market regimes. A naive solution is
to use daily log-returns, which are stationary but discard all long-range memory, and the agent loses any sense of the
broader price trend. \\\\
Fractional differentiation offers a principled middle ground, producing a signal that is stationary while retaining
meaningful historical dependencies.

For each asset $i$, the fractionally differentiated series $\tilde{x}_t^{(i)}$ is
computed as a weighted sum of past prices, where weights decay according to a
fractional parameter $d_i$:

\begin{equation}
    \tilde{x}_t^{(i)} = \sum_{k=0}^{L} \binom{d_i}{k} (-1)^k x_{t-k}^{(i)}
\end{equation}

where $L = 50$ is the truncation window and $\binom{d_i}{k}$ are the binomial
coefficients that govern the rate of memory decay. The parameter $d_i$ is selected
independently for each asset by finding the minimum value that passes the Augmented
Dickey-Fuller (ADF) stationarity test on the training set only, ensuring no
information from the test period influences the transformation. The resulting values,
reported in Table~\ref{tab:fracdiff}, reflect the degree of non-stationarity of
each asset: commodities such as GLD and XOM require relatively low differentiation
($d = 0.35$ and $d = 0.30$ respectively), while technology and financial stocks tend
to require stronger transformations, reflecting their more persistent upward trends.

\begin{table}[h!]
    \centering
    \caption{Fractional differentiation parameter $d$ selected per asset via ADF
    testing on the training set.}
    \label{tab:fracdiff}
    \begin{tabular}{lc|lc|lc}
        \toprule
        \textbf{Asset} & $d$ & \textbf{Asset} & $d$ & \textbf{Asset} & $d$ \\
        \midrule
        MSFT   & 0.75 & JPM  & 0.65 & XOM & 0.30 \\
        GOOG   & 0.65 & V    & 0.75 & CVX & 0.35 \\
        AMZN   & 0.70 & LLY  & 0.75 & UNP & 0.65 \\
        BRK-B  & 0.65 & PG   & 0.65 & CAT & 0.55 \\
        WMT    & 0.60 & GLD  & 0.35 & LIN & 0.65 \\
        COST   & 0.75 & AAPL & 0.65 & NEE & 0.70 \\
        UNH    & 0.75 & AMT  & 0.65 & DBC & 0.55 \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{Model Architecture}
\label{sec:architecture}
A key challenge in this work is that the agent must simultaneously process two fundamentally different types of
input: a sequence of past market prices and the current portfolio allocation, and then produce decisions that are
guided by both the data and financial considerations. To address this, we build a architecture with three main parts:
a dual-stream feature extractor, a PPO actor-critic policy network, and a financial optimization layer that converts
the agent’s output into valid portfolio allocations. Figure~\ref{fig:architecture} illustrates the full pipeline.

\subsubsection*{Feature Extractor}
To process the structured observation space, we implement a custom feature extractor composed of two parallel streams:

\begin{itemize}
    \item \textbf{LSTM stream:} A Long Short-Term Memory (LSTM) network, configured
    with 2 layers, a hidden size of 128, and a dropout rate of 0.2, processes the
    $W \times N$ market history matrix to capture temporal dependencies and momentum
    patterns across the 60-day rolling window. The final hidden state
    $h_T \in \mathbb{R}^{128}$ serves as a compact summary of recent market dynamics.

    \item \textbf{MLP stream:} A single linear layer with a ReLU activation
    (\texttt{mlp\_portfolio}) processes the current portfolio state vector
    $\mathbf{w}_t \in [0,1]^N$, encoding the current allocation into the same
    feature space.
\end{itemize}

The outputs of both streams are concatenated to produce a unified feature vector of dimension $128 + N$, which is then
fed into the PPO actor-critic network.
\subsubsection*{PPO Agent}
The learning agent is built around Proximal Policy Optimization (PPO), an on-policy actor-critic algorithm implemented
via Stable-Baselines3. The actor network takes the combined feature vector as input and outputs a continuous action
$\mathbf{a}_t \in [-1, 1]^N$, interpreted as expected returns $\boldsymbol{\mu}$. The critic network independently
estimates the value of the current state to guide policy updates. PPO constrains the magnitude of each policy update
through a clipped surrogate objective, preventing the agent from making overly aggressive changes that could
destabilize the learned policy.
\subsubsection*{Financial Optimization Layer}
\label{sec:optimizers}
Rather than using the actor's output as portfolio weights directly, we pass it through a financial optimization layer
that solves a constrained optimization problem at each time step. This design ensures that every allocation decision is
by construction consistent with established financial theory. Both optimizers operate over the weight vector $\mathbf{w}
\in \mathbb{R}^N$ subject to the following constraints:

\begin{equation}
    \left\{ w \in \mathbb{R}^n : \sum_{i=1}^n w_i = 1, \quad L \leq w_i \leq U, \quad \forall i \in \{1, \dots, n\} \right\}
\end{equation}

\paragraph{Mean-Variance Optimization (MPT).}
The first optimizer follows Markowitz's mean-variance framework, maximizing expected return penalized by total
portfolio variance:
\begin{equation}
    \begin{aligned}
        \max_{w} \quad & w \mu - \frac{1}{2} \lambda (w^T \Sigma w) \\
    \end{aligned}
\end{equation}

\paragraph{Mean-Semi-deviation Optimization (PMPT).}
The second optimizer replaces the variance penalty with a downside risk term, penalizing only negative portfolio
returns:
\begin{equation}
    \begin{aligned}
        \max_{w} \quad & w \mu - \lambda \sqrt{\frac{1}{N} \sum_{t=1}^{N} \min(0, r_t w)^2 + \epsilon} \\
    \end{aligned}
\end{equation}

where the variables are defined as follows:
\begin{itemize}
    \item $w \in \mathbb{R}^n$ is the \textbf{portfolio weights vector}, representing the proportion of capital allocated to each of the $n$ assets.
    \item $\mu \in \mathbb{R}^n$ is the \textbf{expected returns vector} (output by the PPO Actor).
    \item $\Sigma \in \mathbb{R}^{n \times n}$ is the \textbf{covariance matrix} of asset returns, representing total risk.
    \item $r_t \in \mathbb{R}^n$ denotes the \textbf{realized return vector} at time $t$ within a look-back window of size $N$.
    \item $\lambda > 0$ is the \textbf{risk aversion coefficient}, which scales the penalty for volatility or downside risk.
    \item $L, U$ are the \textbf{lower and upper bounds} for individual asset weights (e.g., $L=0, U=0.1$).
    \item $\epsilon$ is a \textbf{numerical stability constant} ($10^{-5}$) used to ensure the differentiability of the square root in the PMPT objective.
\end{itemize}

%Jacobian for Mean Semi-deviation Utility:
%$-\left[ \mu - \lambda \frac{ \sum_{t=1}^{N} \min(0, r_t w) r_t}{N DR(w)} \right]$ \\
%\textit{Using the Jacobian for the optimization is 27\% faster than without it during the training process.}
%$DR(w)$ = Downside Risk (Mean Semi-deviation) of the portfolio with weights $w$\\

Figure~\ref{fig:architecture} summarizes the full pipeline:
\begin{figure}[h!]
    \centering
    \resizebox{0.95\linewidth}{!}{%
        \begin{tikzpicture}[
    node distance=0.6cm and 0.8cm, % Espacement très serré pour la largeur
    layer/.style={rectangle, draw, fill=blue!10, minimum width=1.8cm, minimum height=0.8cm, text centered, font=\sffamily\tiny},
    input/.style={rectangle, draw, fill=green!10, minimum width=1.6cm, minimum height=0.6cm, font=\sffamily\tiny, rounded corners},
    merge/.style={circle, draw, fill=orange!20, minimum size=0.5cm, font=\sffamily\tiny},
    head/.style={rectangle, draw, fill=red!10, minimum width=1.6cm, minimum height=0.6cm, font=\sffamily\tiny},
    env/.style={draw, fill=gray!5, minimum width=1.8cm, minimum height=0.6cm, font=\sffamily\tiny},
    arrow/.style={-{Stealth[scale=0.8]}, thick}
]

    % --- 1. INPUTS ---
    \node[input] (in_hist) {Price History};
    \node[input, below=0.6cm of in_hist] (in_port) {Portfolio Weights};

    % --- 2. FEATURES EXTRACTOR ---
    \node[layer, right=of in_hist] (lstm) {LSTM};
    \node[layer, right=of in_port] (mlp) {MLP + ReLU};

    % --- 3. CONCATENATION ---
    \node[merge, right=1cm of $(lstm.east)!0.5!(mlp.east)$] (concat) {$\oplus$};
    \node[above=0.05cm of concat, font=\sffamily\tiny\bfseries] {Features Vector};

    % --- 4. AGENT (ACTOR) & SOFTMAX ---
    \node[head, right=2cm of concat] (actor) {Actor};
    \node[layer, right=0.8cm of actor, fill=yellow!15] (softmax) {Objective Optimization};

    % --- 5. CRITIC & REWARD (Placés en dessous pour sauver de l'espace à droite) ---
    \node[env, right=1.5cm of softmax] (reward) {Reward Function};
    \node[head, below=1.2cm of actor] (critic) {Critic};
    \begin{scope}[on background layer]
    \node[draw, fill=red!5, inner sep=10pt, rounded corners,
          fit=(actor) (critic) (softmax), label={[font=\sffamily\bfseries\tiny]above:PPO AGENT (Policy)}] (ppo_box) {};
    \end{scope}

    % --- CONNEXIONS ---
    \draw[arrow] (in_hist) -- (lstm);
    \draw[arrow] (in_port) -- (mlp);

    \draw[arrow] (lstm.east) -- (concat.west);
    \draw[arrow] (mlp.east) -- (concat.west);

    % Flux vers l'Agent et le Critic
    \draw[arrow] (concat.east) -- node[pos=0.25, above, font=\tiny] {State} (actor.west);
    \draw[arrow] (concat.east) -- ++(0.4,0) |- (critic.west);

    % Flux d'exécution
    \draw[arrow] (actor) -- (softmax);
    \draw[arrow] (softmax) -- node[pos=0.5, above, font=\tiny] {Weights} (reward);

    % Boucle temporelle (Next state)
    \draw[arrow, dashed] (softmax.south) -- ++(0,-0.8) -| node[pos=0.25, above, font=\tiny] {} (in_port.south);

    \draw[arrow] (reward.south) |- (critic.east);
    \draw[arrow] (critic.north) -- node[pos=0.5, right, font=\tiny] {Values} (actor.south);

    % Titres de sections
    \node[above=0.6cm of lstm, font=\sffamily\bfseries\tiny] {FEATURE EXTRACTION};

\end{tikzpicture}%
    }
    \caption{Overview of the proposed architecture.}
    \label{fig:architecture}
\end{figure}

\subsection{Reward Function}
\label{sec:reward}

The reward function is the primary mechanism through which the agent learns what constitutes good behavior. Beyond
simply rewarding returns, it must encode the broader objective of this work: discouraging excessive reallocation and
promoting stable, long-term investment strategies. At each time step $t$, the reward $R_t$ is defined as:
\begin{equation}
    R_t = \underbrace{\sum_{i=1}^{n} w_{i,t} \left( \frac{p_{i,t} - p_{i,t-1}}{p_{i,t-1}} \right)}_{\text{Portfolio Return}} - \underbrace{\lambda \sum_{i=1}^{n} |w_{i,t} - w_{i,t-1}|}_{\text{Transaction Penalty}}
\end{equation}
where the variables are defined as follows:

\begin{align*}
    w_{i,t} &\in [0, 0.1] && \text{portfolio weight assigned to asset } i
                              \text{ at time } t \\
    p_{i,t} &\in \mathbb{R}^+ && \text{opening price of asset } i
                                  \text{ at time } t \\
    \lambda &&& \text{penalty factor}
\end{align*}
The first term measures the portfolio's raw return over the period, computed as the weighted sum of individual asset
returns. The second term penalizes large changes in allocation between consecutive steps, using the $\ell_1$ norm of
the weight difference vector. This penalty serves two purposes: it approximates real-world transaction costs, and it
discourages the kind of aggressive, high-frequency reallocation that would be impractical in live trading. Together,
these two terms directly align the agent's incentives with the responsible risk management objective that motivates
this work: the agent is rewarded for generating returns, but only if it does so through purposeful and stable
allocation decisions.

\subsection{Training}
\label{sec:training}
Training is conducted using 8 parallel environments via \texttt{SubprocVecEnv}, which allows the agent to collect
experience across multiple independent market simulations simultaneously and accelerates convergence. Observations
are normalized online using \texttt{VecNormalize}, which standardizes both the observation space and the reward
signal with a running mean and variance estimate, stabilizing training across the wide range of asset return magnitudes.

The PPO agent is trained for a total of 20 million timesteps, with model checkpoints saved every 5 million steps to
allow for post-hoc selection of the best policy.

To account for the randomness in reinforcement learning, coming from both weight initialization and environment
sampling, training is repeated across 4 random seeds. Training progress is monitored via TensorBoard, tracking
portfolio returns, allocation weights, and transaction penalties across episodes. All key hyperparameters are
reported in Table~\ref{tab:hyperparams}.

\begin{table}[h!]
    \centering
    \caption{Training hyperparameters used for all experiments.}
    \label{tab:hyperparams}
    \begin{tabular}{lc}
        \toprule
        \textbf{Hyperparameter} & \textbf{Value} \\
        \midrule
        Total timesteps         & 20,000,000 \\
        Batch size              & 2,048 \\
        Rollout length          & 4,096 \\
        Learning rate           & $5 \times 10^{-5}$ \\
        Optimization epochs     & 20 \\
        Parallel environments   & 8 \\
        Checkpoint frequency    & 5,000,000 steps \\
        \midrule
        LSTM hidden size        & 128 \\
        LSTM layers             & 2 \\
        LSTM dropout            & 0.2 \\
        \bottomrule
    \end{tabular}
\end{table}


\section{Results}
\subsection{Cumulative Return}

Figure~\ref{fig:cumret_new} compares two training configurations, raw and normalized
observation environments, against the equally-weighted buy-and-hold benchmark over
approximately 800 trading days.

The raw environment agent (blue) achieves a cumulative
return of approximately 102\%, outperforming both the normalized environment (orange,
$\sim$83\%) and the benchmark (green, $\sim$84\%). All three curves track closely
through the first 300 steps, after which the raw agent progressively diverges upward,
particularly from step 600 onward where the benchmark experiences a sharper drawdown
that the agent largely avoids. The normalized environment, while producing lower
returns, exhibits noticeably smoother and more stable behaviour throughout the test
horizon, as its curve fluctuates less aggressively and stays closer to the benchmark
trajectory. This stability-performance tradeoff suggests that observation normalization
acts as an implicit regularizer: it reduces the variance of the learned policy at the
cost of some upside capture, and may be preferable in risk-sensitive deployment
settings where consistency matters more than peak return.


\begin{figure}[h!]
    \centering
    \includegraphics[width=0.95\linewidth]{../src/plots/20260327-190807/raw_vs_normalized_mean_cumulative_return.png}
    \caption{Mean cumulative return across seeds: raw vs.\ normalized environment,
             compared against the equally-weighted buy-and-hold benchmark.}
    \label{fig:cumret_new}
\end{figure}

\subsection{Conclusion}
The results presented here demonstrate that coupling a PPO agent with a Post-Modern Portfolio Theory optimization objective offers a valuable allocation policy.
Training is still ongoing, and the next steps will include evaluation across multiple seeds, extending timesteps, and broader ablation studies. Each of these directions not only represents a path to a stronger performance, but also a cleaner understanding of the design choices that truly drive the gains observed so far.


returns of multiple PMPT 20M \
101.54% 116.20% 64.49, 82.50, 90.24%
mean = 90.6 \

returns of multiple PMPT 30M \
79.79%, 86.49%, 55.41%, 71.31%, 75.21%

\newpage


\bibliographystyle{plainnat}
\bibliography{references}
\end{document}